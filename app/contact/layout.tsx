import type { Metadata } from "next";
export const metadata: Metadata = { title: "Contact & Non-urgent Queries", description: "Contact Dr. Milind Mandwar’s team in Patna for consultation availability and non-urgent transplant queries.", alternates: { canonical: "/contact" } };
export default function ContactLayout({ children }: { children: React.ReactNode }) { return children; }
