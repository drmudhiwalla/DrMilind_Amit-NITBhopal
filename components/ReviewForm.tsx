"use client";
import { FormEvent,useState } from "react";
export default function ReviewForm(){const [status,setStatus]=useState('');const [error,setError]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;try{const r=await fetch('/api/review',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const d=await r.json();setError(!r.ok);setStatus(d.message);if(r.ok)form.reset()}catch{setError(true);setStatus('Unable to submit right now. Please try again later.')}}return <form className="review-form" onSubmit={submit}><div className="grid-2"><div className="field"><label htmlFor="r-name">Patient name *</label><input id="r-name" name="name" required maxLength={100}/></div><div className="field"><label htmlFor="r-contact">Email or phone for verification *</label><input id="r-contact" name="contact" required maxLength={150}/></div><div className="field"><label htmlFor="r-rating">Rating *</label><select id="r-rating" name="rating">{[5,4,3,2,1].map(n=><option key={n} value={n}>{n} out of 5</option>)}</select></div><div className="field"><label htmlFor="r-display">Display preference *</label><select id="r-display" name="display"><option>Initials</option><option>Full name</option><option>Anonymous</option></select></div></div><div className="field"><label htmlFor="r-review">Review *</label><textarea id="r-review" name="review" required minLength={20} maxLength={1200}/></div><input name="website" className="trap" tabIndex={-1} aria-hidden="true"/><label className="consent"><input type="checkbox" name="consent" required/> I consent to moderation and, if approved, publication using my selected display preference.</label><p className="muted">Reviews are verified and moderated. Submissions are never published automatically.</p>{status&&<p role="status" className={error?'error':'success'}>{status}</p>}<button className="btn btn-secondary">Submit review for moderation</button><style>{`.review-form{display:grid;gap:18px}.consent{display:flex;gap:10px}.consent input{width:20px;height:20px}.trap{position:absolute;left:-9999px}.muted{color:#5f747b;font-size:.9rem}.success{color:#08725f;font-weight:700}`}</style></form>}
