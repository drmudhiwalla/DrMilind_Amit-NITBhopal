import { doctor } from "@/data/doctor";
export default function BreadcrumbSchema({name,path}:{name:string;path:string}){const data={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:doctor.siteUrl},{"@type":"ListItem",position:2,name,item:`${doctor.siteUrl}${path}`}]};return <script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(data)}}/>}
